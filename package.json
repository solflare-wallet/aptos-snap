{
  "name": "@rise-wallet/aptos-snap",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "start": "mm-snap build && mm-snap serve",
    "build": "mm-snap build",
    "deploy": "npm run build && npm publish"
  },
  "author": "",
  "license": "ISC",
  "files": [
    "dist/",
    "logo.svg",
    "snap.config.json",
    "snap.manifest.json"
  ],
  "dependencies": {
    "@babel/runtime": "^7.21.5",
    "@metamask/key-tree": "^7.0.0",
    "@metamask/snaps-ui": "^0.32.2",
    "tweetnacl": "^1.0.3"
  }
}
